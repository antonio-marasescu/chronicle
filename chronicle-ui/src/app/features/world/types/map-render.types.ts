export type Point = {
  x: number;
  y: number;
};

export type OffsetPoint = {
  offsetX: number;
  offsetY: number;
};

export type DrawMetrics = {
  drawOffsetX: number;
  drawOffsetY: number;
  scale: number;
};
